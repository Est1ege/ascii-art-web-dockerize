FROM golang:alpine

# Создаем рабочую директорию
WORKDIR /app

# Копируем файлы исходного кода в образ
COPY . .

# Компилируем Go приложение 
RUN go build -o server

# Задаем контейнеру порт, который будет открыт
EXPOSE 8080 

# Команда для запуска при старте контейнера
CMD ["./server"] 
